FastAPI is a Python web framework focused on building APIs with clear type hints,
automatic request validation, and interactive OpenAPI documentation.

In this project FastAPI is the HTTP layer. It exposes endpoints such as health,
query, and evaluation while delegating retrieval and model orchestration to other
modules.

FastAPI works especially well here because the application is API-first, local,
and testable with simple client utilities.
